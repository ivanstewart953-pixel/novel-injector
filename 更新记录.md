# Novel Injector 完整更新记录

> 整理截止时间：2026-07-24（Asia/Shanghai）
> 覆盖范围：2026-05-13 至 2026-07-24

---

## 2026-05-13

1. 建立插件第一套完整工作流：上传 `.txt` 小说、识别文本编码、按设定 KB 大小切分原文，并显示文件大小、分段数量和处理进度。
2. 增加清洗 API 配置，包括接口地址、API Key、模型、模型列表拉取、流式输出和基础错误提示。
3. 建立 API 请求限流与并发信号量，减少连续清洗长篇小说时触发服务端频率限制的概率。
4. 完成 AI 清洗压缩流程：每段输出压缩正文，并解析结构化 `ni_meta` 数据。
5. 首次形成剧情节点体系，区分主线 `main`、支线 `sub` 和关键转折 `pivot`，记录标题、正文、时间和地点。
6. 首次形成角色数据体系，提取角色名称、分类、行为摘要和性格依据，并提供手动新增、删除与 AI 生成入口。
7. 增加剧情的时间轴/分类视图，以及按关键转折自动划分阶段、手动新建阶段、分配未归属节点等功能。
8. 增加按阶段选择的 Embedding 向量化、阶段独立索引和基础向量召回注入设置。
9. 增加注入深度、最多召回片段数、相似度阈值等基础参数。

## 2026-05-14

1. 清洗流程增加“暂停”和“跳过当前段”，并完善流式响应为空、响应格式异常、超时和递增等待重试的处理。
2. 增加单次 API 请求超时时间设置，让长文本模型无响应时可自动中止并重试。
3. 剧情节点增加新增、编辑、删除模式；编辑弹窗可修改标题、时间、地点、正文和节点位置。
4. 增加主线/转折与支线之间的 `branch_links` 关联，以及调用 AI 修补关联的入口。
5. 角色人设由简单简介扩展为身份、外貌、性格、关系四类字段，并支持逐字段编辑。
6. 增加按阶段批量开启/关闭角色，以及角色分类内的全选、全不选等批量控制。
7. 增加向量阶段选择面板，可全选、全不选或仅选择尚未向量化的阶段。
8. 新增“当前偏差”分析入口，用当前对话与原著阶段资料对比剧情、人物行为和支线偏离情况。
9. 细化清洗提示词中的主线、支线和关键转折判定标准，限制滥用 `pivot`，并要求记录支线说明和伏笔关联。

## 2026-05-15

1. 增加插件总开关，关闭后可停止主要注入行为，并同步插件启用状态提示。
2. 将注入配置拆分到不同内容类型：角色人设、阶段剧情和向量召回均可分别设置注入位置、深度和消息角色。
3. 增加阶段抽屉，可查看各阶段状态、批量开启/关闭阶段、隐藏空阶段并快速进入阶段内容。
4. 增加小说库快照，可保存、加载和删除当前小说工作区，保留剧情、角色、阶段和处理状态。
5. 增加轻量数据导入/导出入口，以及清除当前向量缓存、清除全部插件数据等维护操作；此时导出尚未包含完整向量，后续于 5 月 23 日升级。
6. 增强角色去重，避免同一人物因姓名、称号或相近记录被重复拆成多个角色。
7. 改进阶段映射：剧情节点和角色首次登场分段可同步映射到用户划分的阶段。
8. 增加时间线排序与阶段定位辅助，方便在长篇节点中按时间或剧情位置检查内容。
9. 重写并扩充 README，开始形成面向安装、清洗、向量化、注入和剧情管理的使用说明。

## 2026-05-16

1. 增加清洗 API 每分钟请求上限，并将流式开关改为更直观的开/关状态显示。
2. 增加“单独清洗此段”，失败分段可以独立重跑，不必重新清洗整本小说。
3. 保存每个分段的 `chunkMeta`，可从已完成分段重建角色和剧情节点，降低中断后结构化数据丢失的风险。
4. 后续分段清洗时可参考上一段压缩摘要，改善跨分段剧情连续性。
5. 增加剧情节点拖拽排序，并保存调整后的显示顺序。
6. 角色页增加原始人设显示/隐藏控制，为后续“原始人设”和“AI 实时人设”分层奠定基础。
7. 增加 AI 批量更新角色人设、单角色更新，以及阶段概括“仅补空白/全部重建”两种方式。
8. 完善失败分段状态和续跑提示，清洗结束后可以从已保存元数据恢复剧情与角色集合。

## 2026-05-17

1. 新增“剧情演绎核心指令”，明确原著节点是参考资料而不是必须照搬的剧本。
2. 强化用户行为优先：用户改变前提后，AI 必须推演连锁反应，不能强行把剧情拉回原著。
3. 增加角色信息边界，要求角色只能依据其已知、已见、已闻或可合理推断的信息行动。
4. 增加每阶段的演绎提示词编辑器、启用开关和恢复默认功能。
5. 明确阶段注入规则：已向量阶段使用语义召回，未向量阶段直接注入阶段剧情，关闭阶段则完全跳过。
6. 重新整理 README，补充安装目录、完整工作流程、剧情类型、阶段划分、向量化和穿书使用说明。

## 2026-05-18

1. 新增全局提示词编辑器，可在所有模型调用前统一加入全局约束。
2. 小说库快照增加重命名和覆盖更新，当前小说状态可持续维护而不是反复新增副本。
3. 大幅重写清洗提示词，明确“压缩正文优先于结构化元数据”，要求完整覆盖剧情、人物关系、时间、地点、因果和转折。
4. 增加缺少 `ni_meta` 时的自动重试与裸 JSON 抢救，降低模型格式漂移造成的整段失败。
5. 细化主线、支线、关键转折的判定，自检每个结构化节点是否在压缩正文中有对应叙述。
6. 扩充 README 中的清洗质量标准、剧情编辑、阶段管理和小说库说明。

## 2026-05-20

1. 未向量化阶段新增两种注入内容：“剧情节点（精简、省 Token）”与“压缩原文（完整、耗 Token）”。
2. 阶段注入会根据所选模式生成不同标签，区分“小说剧情节点”和“小说压缩原文”。
3. 强化清洗输出顺序：必须先输出完整压缩正文，再输出结构化元数据，禁止只给剧情梗概或用节点替代正文。
4. 增加压缩正文最低长度和 10:1～15:1 压缩比约束，避免模型过度缩写导致向量检索缺少细节。

## 2026-05-21

1. 新增世界设定模块，可按分类生成、编辑、启用或关闭世界观资料。
2. 支持一键生成全部世界设定，也可针对单个分类重新生成。
3. 世界设定拥有独立的注入位置、深度和消息角色配置。
4. 默认提取重点从角色行为中分离，强调世界边界、特殊机制、社会规则等稳定设定。
5. 进一步强化压缩正文铁律：每个时间地点和每个结构化节点都应在正文中出现，禁止正文写到一半便提前输出 `ni_meta`。
6. 阶段页补充未向量注入模式说明，清晰区分向量召回、剧情节点直注和压缩原文直注。

## 2026-05-22

1. 增加向量诊断入口，可直接检查 IndexedDB 中实际保存的向量块和阶段分布。
2. 优化世界设定生成区布局，增加更明确的“AI 全部生成”操作。
3. 重写清洗任务定性与角色记录规则，要求所有角色按同一客观标准记录。
4. 明确禁止无依据推导人物动机；口头声明、实际行为和原文旁白揭示的动机必须分开处理。
5. 改进角色归类、身份记录和清洗自检，减少模型将推测内容写入固定人设。
6. 修正阶段/向量状态相关细节，并增加查看实际存储数据的调试能力。

## 2026-05-23

1. 向量数据改为二进制存储，降低浏览器存储体积并提升持久化效率。
2. 导出/导入升级为 ZIP 备份，除轻量设置外可携带向量数据，用于跨设备迁移；仍兼容旧版 JSON 导入。
3. 增加重数据服务端文件读写、删除和恢复，将大型正文/分段数据与轻量设置分离。
4. 增加小说与向量数据指纹校验，启动时可核对 IndexedDB 中的向量是否属于当前小说。
5. 增加向量 API 独立每分钟请求限制，以及“暂时关闭向量注入但保留向量数据”的开关。
6. 角色字段增加性别、角色分类和首次登场阶段；手动新增与编辑角色时可直接设置这些信息。
7. 完善服务端重数据失败提示和清理逻辑，避免轻量快照存在但大文件缺失时静默失败。

## 2026-05-24

1. 新增文风分析模块，支持“从原著分段采样”和“手动粘贴范文”两种来源。
2. 文风分析可设置采样字数、采样分段、自定义提示词，并将生成结果作为可编辑的“文风执行指南”保存和注入。
3. 新增穿书模式基础框架，可开启/关闭并在正文状态栏显示阶段、当前节点和完成状态。
4. 增加穿书推进提示词、持续提示词和后文推演提示词，节点切换后可引导 AI 进入下一叙事阶段。
5. 增加节点完成勾选、阶段列表、节点列表、阶段锁定和已完成节点归档。
6. 后文推演可生成三条不同方向的剧情建议，为之后的“点击填入输入框”功能打下基础。
7. 完善支线、伏笔和阶段之间的联动，避免未到阶段的后续节点过早注入。
8. 完成草莓抹茶风格的配色与界面润色，统一开关、提示和状态颜色。

## 2026-05-25

1. 将穿书提示词明确拆分为“节点切换时一次性注入”的推进提示词和“每条消息持续注入”的当前阶段提示词。
2. 增加第一阶段开场提示，故事开始时可直接建立当前叙事阶段。
3. 重写当前阶段规则：节点时间和地点只是代表性锚点，不能把人物永久锁死在该时空。
4. 强化用户干预后的三步推演：锚定现状、推演连锁、自适应改写后续节点。
5. 明确阶段完成必须由用户确认，AI 不得擅自宣布完成或自动替用户推进关键选择。
6. 区分主线、支线和关键转折在穿书中的处理方式，允许支线自然跳过、关键转折随用户改写。
7. 优化穿书开关、提示词面板和状态文案，并统一新版配色。

## 2026-05-26

1. 穿书模式新增可选显示方式：正文状态栏或独立弹窗。
2. 新增“小票/留台单”风格穿书弹窗，包括阶段下拉、节点列表、当前节点高亮、已完成/待完成统计和进度显示。
3. 弹窗支持拖动、打开/关闭、上一节点/下一节点、暂停/恢复和后文推演。
4. 节点列表显示主线、支线、关键转折及伏笔信息，并可在弹窗内勾选完成状态。
5. 优化推演 `desc` 生成规范，要求建议可执行、有具体动作和场景，禁止空泛情绪标签、极端副词和套路化意象。
6. 扩充 README，对世界设定、文风分析、注入类型和穿书完整流程进行系统说明。
7. 增加穿书提示词授权说明，并整理清洗/净化规则相关界面。

## 2026-05-27

1. 修正插件总开关与穿书模式的状态文字和标题布局。
2. 修复触屏/鼠标操作穿书弹窗时页面跟随滚动的问题，使拖动和节点操作优先。
3. 补充阶段概括为空时的占位显示，避免空白卡片造成误判。
4. 更新导出说明，将文风数据明确列入备份内容。
5. 清理误打包的临时样式备份文件并缩减安装包体积。

## 2026-05-28

1. 当日版本与 2026-05-27 最终快照核心文件哈希完全一致。
2. 本次属于重新打包/复制发布，没有发现可确认的功能、提示词或界面变化。

## 2026-05-29

1. 向量化增加完整性检查：阶段只有在所有应有向量块成功写入后才标记为完成。
2. 增加“补全缺失”按钮，自动对比应有块与 IndexedDB 实际数据，只重新请求缺失部分。
3. 向量诊断增加失败阶段、缺失块、API 配置和限速问题的具体提示。
4. 增加“召回时提取的消息内容”标签配置，可只提取 `<content>`、`<summary>` 等 XML 标签内文字；没有标签时自动回退完整消息。
5. 增加“取近几条对话”，多条消息分别计算 Embedding，并按时间远近指数衰减加权，近期消息权重更高。
6. 为部分 Embedding 模型增加 Instruct/Query 查询前缀，改善语义召回匹配。
7. 穿书模式增加“归纳”开关，要求 AI 每轮生成隐藏的 `<ni_query>人物 | 地点 | 事件</ni_query>`，并自动用于下一轮召回。
8. 向量查询可附带当前穿书节点的标题、时间和地点，提升当前场景相关性。
9. 改进阶段节点跳转和向量失败状态，避免 API 失败后仍显示“已向量”。

## 2026-05-30

1. 仅更新 README，没有运行时代码变化。
2. 补充向量诊断、缺失块补全、消息标签提取、近几条对话和加权召回的完整说明。
3. 补充阶段卡片节点展开、阶段批量开启/关闭等操作说明。

## 2026-06-03

1. 扩展演绎/写作提示词中的禁词和禁止句式列表。
2. 要求用可感知动作、身体反应和场景细节替代“微微、极度、狡黠、低哑、眼底透着”等抽象或套路化标签。
3. 禁止“不是……而是……”“不像……倒像……”“仿佛在说”等固定句式堆叠，减少模型腔。
4. 当日只修改提示词文本，没有新增界面控件或数据结构。

## 2026-06-06

1. 将插件正式导入 GitHub，建立后续可追踪的云端历史；首次提交包含主程序、模板、样式、说明和基础工具模块。
2. 将清洗正文、分段元数据等重数据拆分到服务端文件，轻量设置只保留索引，启动时按需异步加载。
3. 增加重数据多文件名兼容、缺失数据恢复、阶段重建和向量状态与 IndexedDB 自动对账。
4. 穿书模式增加“归纳”和“沉浸”提示词；沉浸模式限制正文只写 `<user>` 已感知、被告知或可合理推断的信息。
5. 改写角色演绎提示词，强调当前对话事实高于原著资料、禁止无故黑暗化和冲突升级，并保护用户在当前分支中的主动权。
6. 新增拼音、存储、向量和 ZIP 等工具模块，为后续拆分主文件做准备。
7. README 改为适合 GitHub 发布的安装、快速开始、API 配置和功能说明。

## 2026-06-07

1. 将后文推演结果改为可直接采用的下一步行动选项，点击整条建议即可填入 SillyTavern 输入框。
2. 重写推演格式，固定输出情感向、张力向、伏笔向三项，限制标题长度并要求 `desc` 是可执行的下一轮输入，而非作者视角分析。
3. 增加归纳提示词、沉浸提示词的独立编辑区，并统一穿书设置中的自定义复选框样式。
4. 新增角色别名数据合并和“用户代入角色”功能，可选择角色及称呼，在注入层将其替换为 `<user>`。
5. 用户代入配置按聊天保存，可手动新增、删除称呼，并按称呼出现阶段显示候选。
6. 改进支线关联编辑：可选择关联的主线/转折，也可指定剧情节点插入位置。
7. 大幅重写 README，使用户代入、剧情关联、穿书和注入设置与代码保持一致。

## 2026-06-08

1. 新增完整外观主题编辑器，可调整主色、次色、强调色、警示色、背景色和文字色。
2. 每个颜色同时支持系统取色器和十六进制文本输入，输入后自动规范为 `#RRGGBB`。
3. 增加主题预设的新建、保存、删除、导入和导出，允许覆盖当前预设而不是不断生成副本。
4. 增加内置“星糖梦簿”和夜间配色，并调整夜间主题颜色、渐变背景和玻璃效果。
5. 增加“跟随美化”“去除边框”“去除卡片”和“状态栏跟随”等外观开关。
6. 将主题编辑逻辑拆分到 `theme-editor`、`theme-utils`、`statusbar-theme` 等模块，减少主文件耦合。
7. 穿书状态栏可跟随当前主题变量，主题切换时同步刷新。
8. 新增全局尾部提示词，可与头部提示词分别设置注入位置、深度和消息角色。
9. 优化向量化不完整状态：没有可向量化文本或仅部分完成时不再误显示为成功。
10. 统一各开关的“开/关”状态显示和主题色反馈。

## 2026-06-09

1. 调整角色演绎提示词：原著事件只能作为历史惯性，需要 `<user>` 参与、同意或执行的事件，必须等待用户明确输入后才能发生。
2. 明确禁止 AI 擅自替 `<user>` 说出原作台词、执行原作行为或继承原角色选择。
3. 增加用户角色边界提示，区分 `<user>`、原著主角和其他 NPC，减少身份错误映射。
4. 优化穿书开关与插件总开关同步：关闭插件时暂停穿书，再次开启时恢复原状态。
5. 新增穿书“轻量化召回”，根据当前节点的原文分段、时间和标题裁掉明显属于未来剧情的向量召回内容。
6. 轻量化召回不重新向量化、不修改原文，仅在输出召回内容前进行裁切。
7. 无边框主题继续保留输入框、下拉框和小说卡片等必要控件的弱边框，避免界面不可辨认。

## 2026-06-11

1. 全局提示词增加三种来源模式：“跟随酒馆预设”“内置提示词”“不使用”。
2. 跟随酒馆预设时读取当前 SillyTavern 主预设结构，但实际请求仍使用插件自己的 API URL、Key 和模型。
3. 增加酒馆预设消息顺序解析、变量/宏替换和安全沙箱处理，避免后台工具调用污染当前角色卡变量。
4. 增加后台任务身份包装，明确清洗、世界设定等请求不是前台 RP/续写，减少预设把工具任务误当剧情生成。
5. 改进流式 Chat Completions 响应解析和返回文本提取，兼容更多接口返回格式。
6. 清洗按钮增加部分进度判断：已完成部分分段时，可区分重新清洗与续跑，并在重数据缺失时阻止危险覆盖。
7. 主题细节修正：无边框模式下当前小说卡片边框使用所选主题主色。

## 2026-06-12

1. 重构 AI 实时人设生成，批量更新和单角色更新共用统一的提示词、上下文、解析和重试流程。
2. 检测模型因长度上限截断、空响应、非完整 JSON 或字段结构异常，并自动重试最多 3 次。
3. 多次失败后显示具体角色和失败原因，不再只给笼统错误。
4. AI 实时人设只写入独立字段，不覆盖清洗得到的原始人设。
5. 单角色更新按钮改为更直观的星光图标，并补充可访问性标签和 README 说明。

## 2026-06-14

1. 为剧情节点生成稳定节点 ID，记录原分段、原始顺序、手动顺序和剧情类型。
2. 清洗重建、阶段重建和节点编辑后保留用户手动排序，避免节点突然回到模型输出顺序。
3. 删除、新增、移动节点后重建阶段映射，尽量保持节点原有阶段归属。
4. 统一主线、支线、转折的剧情顺序比较器，时间轴、分类页、阶段页和跳转使用同一顺序。
5. 改进剧情节点 UI 反馈，跳转到节点时提供更明显的定位和高亮。
6. 向量召回结果在完成语义筛选后按原故事顺序输出，避免召回片段顺序打乱叙事。

## 2026-06-18

1. 修复部分清洗完成后“重新/续跑”按钮布局错位。
2. 明确显示已完成分段数、失败分段数，并提示左侧重新清洗、右侧继续处理。
3. 统一模板、样式和运行时按钮状态，避免窄屏下按钮重叠。

## 2026-06-21

1. 新增角色“自动休眠”：切换到某阶段时，自动关闭该阶段正文中未出现的非主角角色注入。
2. 主角和当前用户代入角色受到保护，不会被自动休眠。
3. 角色卡增加“自动休眠”标识和状态说明，可随时关闭该功能。
4. 将前文偏差分析扩展为可持久化的“当前偏差”资料，可保存并参与后续注入。
5. 新增偏差注入设置，支持独立配置注入位置、深度和消息角色。
6. 增加偏差提示词编辑、结果展开/折叠和按钮状态同步。
7. 偏差分析会从当前阶段原著资料和聊天内容中整理已改变事实、当前约束与仍有效的原著事实。

## 2026-06-22

1. 偏差分析增加设置面板，可开启自动更新并设置每隔多少层对话运行一次。
2. 手动偏差分析可设置最近消息数量，避免每次都发送全部聊天记录。
3. 自动更新会检查当前楼层数和上次覆盖范围，达到间隔后再运行。
4. 改进偏差分析无可用阶段、无向量召回、无节点/概括时的具体提示。
5. 运行中统一显示加载状态，避免重复点击启动多个偏差请求。
6. 穿书暂停/继续状态在正文状态栏、弹窗和设置面板之间同步。
7. 调整复选框、按钮和主题控制样式，使插件控制区更一致。

## 2026-06-23

1. 修复偏差按钮运行时仍显示旧结果状态的问题，执行期间只显示加载反馈。
2. 增加偏差覆盖楼层和最近处理范围，结果标题显示“已总结 X 层”。
3. 支持只分析新增楼层并与旧偏差合并，避免每次重算整段聊天。
4. 增加偏差范围重试按钮，可重跑最近失败或需要修正的区间。
5. AI 实时人设批量更新支持取消，取消时中止请求、重试等待和后续角色任务。
6. 统一 AbortController 超时与用户取消错误，按钮会显示“取消更新/取消中”。
7. 偏差状态改为按聊天保存，不再让不同聊天共享同一份偏差；旧全局数据可迁移到当前聊天。

## 2026-06-25

1. 将当前偏差拆分为三个可编辑区：“已改变事实”“当前偏差约束”“仍保留的原著事实”。
2. 偏差更新会合并新旧区间，保留仍有效的硬事实，并对已被新正文推翻的事实进行更新。
3. 改进偏差楼层进度显示，区分已覆盖层数与当前聊天总层数。
4. 用户代入角色增加“替换模式”和“扮演模式”，并针对两种语义生成不同身份边界提示。
5. 增加用户代入提示词预览、编辑和状态提示，便于确认实际会注入给模型的身份说明。
6. 主题编辑器增加图标替换开关，可在默认图标与自定义图标风格间切换。

## 2026-06-26

1. 优化穿书当前节点和可推进边界，切换阶段或数据更新后自动校正当前节点，避免落在已失效位置。
2. 支线节点的阶段归属可根据关联的主线/转折推导，减少支线被分配到错误阶段。
3. 调整穿书前沿阶段推进逻辑，只有满足当前进度条件时才开放后续阶段。
4. 改进向量召回消息提取，统一 XML 标签块、消息角色和可计数消息的判断。
5. 用户代入的替换模式、扮演模式和关闭状态各自拥有独立提示词，可分别编辑和保存。
6. 角色页进一步区分“原始人设”和“AI 实时人设”，关闭注入时显示明确占位文字。

## 2026-06-27

1. AI 实时人设更新增加目标角色校验：近期对话中没有出现目标角色或可靠别名时，保留原人设而不是凭空生成。
2. 检查模型返回的人物是否与目标角色匹配，避免批量更新时串角色。
3. 完善批量更新的完成、跳过、取消和失败统计；单角色失败会显示角色名与具体原因。
4. 修复偏差楼层追踪，统一使用 SillyTavern `mes_id` 与实际聊天楼层，处理可见但标记为 system 的消息。
5. 修复手动更新、自动更新和范围重试的进度文字，明确当前分析或重试的楼层区间。
6. 自动更新开启后可补跑此前漏掉的区间，并在设置开关、倒计时和提示文字之间保持同步。
7. 增加自动更新间隔始终可见，关闭自动更新后仍可提前调整间隔。
8. 点击穿书弹窗节点行可展开/收起节点摘要，勾选区继续负责完成状态。
9. 改进用户代入映射：区分姓名类别名与身份/礼貌称谓，避免把“殿下、师兄”等称谓错误替换成独立姓名。
10. 用户代入替换只在注入文本和最终提示中生效，保护 `<...>` 标签内部内容，并避免原角色和 `<user>` 被模型理解为两个人。

## 2026-07-01

1. AI 实时人设改为按聊天保存，同一小说在不同聊天中可以拥有不同的即时人物状态。
2. 角色原始数据不再直接保存聊天中的 AI 实时人设，避免小说快照把一个聊天的临时状态带到另一个聊天。
3. 用户代入处于“替换模式”时，所选原角色不会再作为独立 NPC 更新或注入 AI 人设。
4. 用户代入处于“扮演模式”时保留该角色的人设资料，作为 `<user>` 的既有角色基础使用。
5. 角色编辑区增加 AI 身份、外貌、性格和关系字段的独立保存与显示状态。

## 2026-07-04

1. 清洗 API 与 Embedding API 增加独立并发数设置。
2. 清洗、世界设定、文风等普通生成请求使用清洗并发信号量；向量化和向量召回使用独立向量并发信号量。
3. 并发值 `1` 表示串行，`0` 为兼容旧配置也按串行处理，大于 `1` 时限制最大并发请求数。
4. 阶段向量化与“补全缺失”可按设置并行处理多个文本块，同时继续受每分钟请求上限控制。
5. 配置区布局改为可换行，适配新增的限速、并发和超时输入项。

## 2026-07-08

1. 新增顶栏书本图标显示开关，可从 SillyTavern 扩展菜单隐藏或重新显示插件入口。
2. 隐藏顶栏图标时会先关闭已打开的插件抽屉，并同步扩展菜单中的开/关状态。
3. 增加图标字体保护，防止字体美化插件把 Tabler Icons 或 Font Awesome 替换成正文字体导致图标乱码。
4. 当日初版顶栏开关曾因实现问题回滚，随后以更精简的事件绑定和样式方案重新加入；最终版本保留该功能。
5. 小说上传新增 `.mobi` 支持，可解析未加密的无压缩或 PalmDOC 压缩 MOBI，处理常见编码、HTML 正文和尾部记录。
6. 对 DRM/加密 MOBI、Huff/CDIC 压缩、损坏记录表和不支持的压缩类型给出明确错误，建议转换为 TXT 后上传。
7. 上传区改为同时接受 `.txt` 和 `.mobi`，并统一文件类型校验与正文提取流程。

## 2026-07-09

1. 修复顶栏图标隐藏后再开启时，抽屉内容仍残留内联 `display:none`、导致点击图标打不开面板的问题。
2. 重新显示顶栏入口时会移除旧的隐藏样式，恢复正常开关抽屉。

## 2026-07-11

1. 世界设定生成检测输出被 Token 上限截断时自动重试，并在按钮上显示“输出截断，重试中”。
2. 将清洗响应解析、分段布局、剧情顺序、阶段映射、向量状态、并发和请求限速等逻辑拆分为可独立验证的工具模块。
3. 调整分段大小时尽量保留已完成分段的边界、压缩结果和元数据，只重新切分尚未完成的连续正文。
4. 如果无法完整恢复已完成分段，则取消重新分段并保留旧设置，避免丢失清洗进度。
5. 清洗暂停、跳过、重试和并发进度显示统一使用新的状态统计。
6. 对受影响阶段重置向量状态，并提供向量源分段索引重映射，降低重新分段后向量错位风险。

## 2026-07-12

1. 将大型运行时进一步整合为业务域模块：API、清洗、提示词、存储、剧情数据、穿书、UI、向量和世界设定；`index.js` 主要负责状态与事件编排。
2. 清洗、阶段概括和角色 AI 人设统一使用同一普通生成并发上限，并显示并发 worker 与完成进度。
3. 重建剧情和角色数据时保存源快照、稳定 ID、手动排序、阶段归属和用户编辑，避免后续清洗把已改内容覆盖或复活已删除节点。
4. 修复清洗中断、重试、跳过和结构化数据重建的状态同步。
5. 固定插件抽屉高度与内部滚动，减少切换页面或展开面板时抽屉抖动。
6. 向量阶段选择在界面重新渲染后继续保留，不再意外清空勾选。
7. 世界设定支持新增/删除分类，并增加统一提示词管理面板和每分类提示词保存。
8. 修复切换小说快照后旧小说的清洗正文、剧情、角色或阶段状态残留到新小说的问题。

## 2026-07-13

1. 穿书状态栏和小票弹窗的阶段列表增加最大高度、独立滚动条和触屏滚动，长阶段列表不再撑出屏幕。
2. 节点列表同样限制高度并优化滚动，移动端和窄屏操作更稳定。
3. 点击未激活节点行会切换当前节点；点击右侧勾选只改变归档状态，两种操作不再混淆。
4. 直接跳到同一阶段较后的节点时，自动把此前尚未完成的同阶段节点标记为完成，并同步前沿阶段和进度。
5. 切换当前节点后重新渲染状态栏/弹窗，确保高亮、说明、统计和滚动位置一致。

## 2026-07-15

1. 新增可选“自动保存”，默认关闭，需要用户手动开启。
2. 开启后首次立即保存；后续内容变化经短暂防抖后自动更新当前小说快照。
3. 如果当前小说尚未进入小说库，自动以源文件名创建快照；已有快照则静默覆盖更新。
4. 自动保存失败只记录警告，不中断清洗或用户操作；关闭开关时取消等待中的保存任务。
5. 修复偏差自动更新倒计时，聊天楼层变化后及时刷新“还差多少层”的状态提示。
6. 向量诊断改为独立模态框，移动端可完整滚动查看长报告，不再被系统 `alert` 截断。

## 2026-07-17

1. 关键转折编辑增加“位于主线之后”选择器，可搜索并指定其应跟随的主线节点，或保留当前位置/追加到末尾。
2. 支线编辑使用同一选择器选择“关联剧情”，统一主线、转折和支线的关系编辑体验。
3. 剧情关联和移动后会刷新字段标签、顺序和阶段映射，减少只改界面未改运行时状态的问题。
4. 向量阶段勾选按小说保存，重新渲染、补全诊断或切换面板后仍能恢复选择。
5. 世界设定分类的展开/编辑等界面状态在重新渲染后保持。
6. 偏差自动更新状态使用当前聊天实际楼层刷新，倒计时更准确。
7. 新增节点感知向量召回：在语义相似度基础上参考当前穿书节点与候选片段的距离，对附近剧情提高权重、远处节点降低权重。
8. 阶段注入 UI 会根据是否有向量、是否临时关闭向量注入等状态，正确显示向量开关或未向量注入模式。

## 2026-07-18

1. 用户代入角色配置统一为按聊天持久化的结构，保存开关、模式、角色、称呼和勾选状态，并兼容旧数据迁移。
2. 切换用户代入配置后同步刷新角色注入、演绎提示词、预览和相关运行时状态。
3. 修复穿书状态栏与小票弹窗切换当前节点时，聊天中保存的节点索引不同步的问题。
4. 规范未向量注入模式和穿书状态读取，减少加载旧快照时出现无效值。
5. 调整全局提示词面板 DOM 位置，使面板始终显示在其按钮/标题下方，不再漂移到其他设置区域。

## 2026-07-19

1. 将偏差结果升级为结构化“当前分支事实”笔记本，不再只保存三段大文本。
2. 每条事实拥有稳定 ID、类型、正文、创建楼层、更新楼层和状态，可独立新增、编辑和保存。
3. 偏差更新会比较新旧事实，生成新增、更新和移除记录；未被推翻的硬事实继续保留。
4. 增加“变更记录”面板，可查看事实历史并一键清除本地记录；历史仅用于用户查看，不参与注入。
5. 当前偏差正文默认折叠，标题区显示事实列表、编辑和历史入口，减少剧情页占用空间。
6. 统一 AI 实时人设关闭注入时的灰色占位样式，编辑态和展示态表现一致。
7. 剧情页操作工具栏改为粘性定位，滚动长节点列表时新增、编辑、删除等操作仍保持可见。
8. 增加纯 JavaScript SHA-256 回退，在非安全上下文或浏览器没有 Web Crypto 时仍可计算小说指纹并上传文件。

## 2026-07-20

1. 将“查看某节点”和“真正推进到某节点”拆成独立状态。
2. 用户可在穿书状态栏/弹窗中预览其他节点的标题、正文和阶段，而不会立即改变当前剧情进度。
3. 只有明确执行推进/选择操作时才提交新的当前节点和归档进度。
4. 增加“视图重置到当前节点”的协调逻辑，重新打开或刷新时回到真实进度，避免预览状态被误保存。

## 2026-07-21

1. 修正偏差事实楼层来源：优先取事实真正首次出现在聊天正文中的楼层，而不是自动总结触发楼层或分析范围末尾。
2. 区分事实的记录楼层、更新楼层和来源楼层；事实被正文改写后显示“更新于第 X 层”。
3. 改进事实合并，保留未被推翻事实的原始来源，同时为修正后的事实记录最新更新位置。
4. 偏差事实类型增加更清晰的中文标签，区分事件、状态、关系、世界规则、剧情、角色、地点、支线和一般事实。
5. Embedding 向量化增加批量请求，可一次提交多个文本块，默认每批 16 块。
6. 配置区增加每批 1、4、8、16、20、32 块的选择；批量大小与向量并发、每分钟限速可以同时使用。
7. 正常向量化和“补全缺失”都改为批次处理，显著减少请求次数。
8. 批量 API 失败时按整批标记失败；API 成功但单块写入 IndexedDB 失败时单独记录该块，进度统计继续保持准确。

## 2026-07-22

1. 优化向量批量大小选择器，将控件宽度压缩并居中显示，减少配置行占用。
2. 移除浏览器原生下拉箭头和背景图，统一不同浏览器下的紧凑外观。
3. 加强跨设备 ZIP 隐私保护：不再导出清洗/向量 API 的地址、模型或 Key，向量来源仅保存不可逆的 `SHA-256` 指纹摘要。
4. 导入 ZIP 或加载小说快照时保留本机 API 配置；旧版明文向量指纹会在导入时自动摘要化，不再把敏感来源信息写入新备份。
5. 向量指纹改为仅供识别与诊断，不再作为兼容门槛；切换当前 Embedding 模型不会清空、停用已有向量，也不会阻止已有阶段恢复“已向量”状态。
6. 召回时按实际向量长度比较维度：维度一致的旧向量继续参与召回，维度不一致的块自动排除，即使相似度阈值设为 `0` 也不会误用。
7. 向量化标题右侧增加动态兼容提示，可显示混合来源、混合维度、当前来源不同及最近查询维度不匹配；诊断窗口同步显示数据库维度与最近查询维度。
8. 修复移动端向量提示图标被单独挤到上一行的问题，让提示图标与备注文字首行按基线对齐，并将警示感叹号向下微调 `1px`。
9. 向量阶段选择器首次打开时默认排除已经向量化的阶段；用户手动调整勾选后仍保留本次会话中的选择。
10. 已向量阶段若来源与当前向量配置不同，会显示警示色“异源向量”标记；该标记仅作诊断，旧向量仍可使用。

## 2026-07-24

1. 修复旧聊天中偏差事实来源楼层被误显示为本次总结触发层的问题；事实现在优先显示其首次在聊天正文中真正成立的楼层。
2. 兼容解析 `[source_floor: N]`、圆括号/中文括号、中文“来源楼层/证据楼层”以及裸标签等旧格式，并从事实正文中清理这些内部标记。
3. 加载旧聊天时会结合事实变更历史和异常的创建/更新楼层恢复真实来源层，统一修正 `sourceFloor`、`createdFloor` 与 `updatedFloor`；迁移到来源层数据版本 2 后只自动回写一次。
4. 当前分支事实列表和后续偏差分析上下文统一优先使用真实来源层；事实后来被正文改写时，继续单独显示“更新于第 X 层”。
5. 修复旧版 `vecInjDisabled` 残留可能意外关闭向量召回的问题；现在只有用户明确点击关闭按钮后，插件才会停用向量注入。
6. 新增统一的阶段注入计划模块，使正常上下文注入与偏差分析采用相同的已向量/未向量阶段划分；用户主动关闭向量时，已开启阶段会回退为直接阶段资料注入。
7. 升级原著剧情向量召回，将当前剧情与原著前情拆分为独立候选池并分别保留召回名额，避免跨阶段的重要事件被当前阶段片段挤出。
8. 最近聊天消息与当前剧情节点会共同组成检索引子，用于定位可能相关的原著内容；最终注入的召回结果仍只来自已生成的原著向量块。
9. 当前开启阶段尚未向量化时，只要更早阶段存在可用向量且用户未主动关闭向量调用，仍会继续召回原著前情；当前阶段则继续使用原有的未向量注入方式。
10. 普通模式以最高开启阶段向前的连续开启区间作为当前窗口，更早阶段作为前情；穿书模式严格以当前节点所在阶段为边界，不搜索或注入未来阶段。
11. 当前剧情池继续结合节点距离排序，原著前情池不施加距离衰减，并增加人名与线索词辅助加权，让跨阶段的死亡、承诺、秘密和因果线索更容易在后续剧情中重新召回。
12. 每轮会把多个检索引子合并为一次批量 Embedding 请求，并只读取一次向量数据库，减少重复请求与存储访问。
13. 偏差分析同步采用相同的阶段边界和原著前情召回规则，使正常剧情注入与偏差分析保持一致。
14. 完成当前/前情名额隔离、未来阶段排除、仅前情向量可用时继续召回，以及单轮一次批量请求和一次数据库读取的聚焦验证。
15. 修复同一原文分段被多个阶段关联时跨池重复注入的问题；按源分段索引和长文本内容去重，当前与前情区块不再重复显示同一原文。
16. 轻量化召回增加当前节点边界定位：在同一清洗分段内继续定位向量子块，过滤当前节点之后的同段子块、后续清洗分段及前情池中重复带入的后文；同一子块内出现后续节点时同步裁剪。
17. 穿书模式下未向量阶段的直接注入及相关剧情分析参考仅保留当前节点所属阶段，避免未来阶段资料进入本轮请求。

---

## 当前状态摘要（截至 2026-07-24）

1. 最新云端版本支持 TXT 与部分 MOBI 小说导入、断点清洗、分段并发、结构化剧情/角色提取、世界设定和文风生成。
2. 支持按阶段直接注入剧情节点/压缩原文，或使用带并发、批量、限速、完整性检查和原著前情独立名额的 Embedding 向量化与召回。
3. 支持穿书节点进度、预览与推进分离、归纳/沉浸/轻量化召回、节点感知召回、未来阶段隔离和后文行动建议。
4. 支持用户代入角色、角色别名、按聊天保存的 AI 实时人设、自动休眠和可取消的批量更新。
5. 支持按聊天保存的结构化偏差事实、增量自动更新、真实来源楼层追踪、旧聊天来源层迁移、变更历史和偏差注入。
6. 支持小说库、可选自动保存、ZIP/旧 JSON 导入导出、服务端重数据拆分、IndexedDB 向量诊断与缺失补全。
7. 支持完整主题编辑、主题预设、状态栏跟随、顶栏图标显隐和移动端长内容查看。
